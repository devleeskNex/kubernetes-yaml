apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: billing-kb-deployment
  namespace: nexclipper
  labels:
    app: billing-kb-deployment
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: billing-kb-pod
    spec:
      containers:
      - name: billing-kb-container
        image: sppark/killbill:0.20.2
        env:
        - name: KILLBILL_DAO_URL
          value: jdbc:mysql://billing-db-service.nexclipper:32001/killbill
        - name: KILLBILL_DAO_USER
          value: root
        - name: KILLBILL_DAO_PASSWORD
          value: killbill
        - name: TEST_MODE
          value: "false"
        ports:
        - name: kb
          containerPort: 8080

